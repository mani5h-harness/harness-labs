# service.yaml
apiVersion: v1
kind: Service
metadata:
  name: my-app-service
spec:
  selector:
    app: my-app
    environment: {{ .Values.color }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5678
  type: ClusterIP
