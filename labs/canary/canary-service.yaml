apiVersion: v1
kind: Service
metadata:
  name: rolling-example-service
spec:
  selector:
    app: rolling-example
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5678
  type: LoadBalancer
